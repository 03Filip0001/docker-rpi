<VirtualHost *:80>

	SetEnv HOST_NAME ${HOST_NAME}
	SetEnv SERVER ${SERVER}

	ServerName plex.${HOST_NAME}

	ProxyPreserveHost On
	ProxyPass "/" "http://${SERVER}:1003"
	ProxyPassReverse "/" "http://${SERVER}:1003"

</VirtualHost>
